#include <stdio.h>

int main() {
    int n, count = 0, num = 2;
    int sum = 0;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    while (count < n) {
        sum += num;
        num += 2;  // next even number
        count++;
    }

    printf("Sum of first %d even numbers = %d\n", n, sum);

    return 0;
}